<UserControl x:Class="ICSProject.App.Controls.PrizeTag"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      x:Name="Self"
      mc:Ignorable="d" 
      d:DesignHeight="50" d:DesignWidth="100">

    <Grid DataContext="{Binding ElementName=Self}">
        <Border Style="{StaticResource PrizeTagControl}">
            <!--<i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseOver">
                    <i:InvokeCommandAction Command="{Binding Cmd}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>-->
            <TextBlock Text="{Binding Txt}"/>
        </Border>
    </Grid>

</UserControl>
